<UserControl x:Class="NTRCheck.Views.Test"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NTRCheck.Views"
             mc:Ignorable="d" VerticalAlignment="Center"
             Background="LightGray" Foreground="White">
	<UserControl.Resources>
		<Style x:Key="tagBorderStyle" TargetType="{x:Type Border}">
			<Setter Property="BorderBrush" Value="#55000000"/>
			<Setter Property="BorderThickness" Value="1"/>
		</Style>
	</UserControl.Resources>
	<Border Style="{StaticResource tagBorderStyle}">
		
		<TextBlock Margin="12,0,12,0" FontSize="8" Text="Tag" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="Verdana"/>
	</Border>
</UserControl>
